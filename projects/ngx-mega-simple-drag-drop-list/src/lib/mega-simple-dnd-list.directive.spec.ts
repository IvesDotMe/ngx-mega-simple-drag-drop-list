import { MegaSimpleDndListDirective } from './mega-simple-dnd-list.directive';

describe('MegaSimpleDndListDirective', () => {
	it('should create an instance', () => {
		const directive = new MegaSimpleDndListDirective();
		expect(directive).toBeTruthy();
	});
});
